// Arquivo de tipagem do repositório de produtos
// Define os métodos que o repositório deve implementar

type ProductRepository = {
    id: number
    name: string
    price: number
    created_at: number
    updated_at: number
}